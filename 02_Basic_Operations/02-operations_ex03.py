# Basic Operations chapter, exercise 3:
# What does the following code do? Why?

print('5' + '10')

# It prints '510' because '5' and '10' are strings, so the + operator
# concatenates the strings instead of adding integers, as one might mistake
# them for at first glance.